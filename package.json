{
  "name": "photo-tag-pro",
  "version": "1.0.0",
  "description": "Photo Tag Pro workspace",
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npx concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
